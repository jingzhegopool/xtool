# 推送文档 — Git 推送到 GitHub 操作指南

> 适用于 `xu` 主机（Windows），网络环境需要代理才能访问 GitHub。

---

## 1. 配置 Git 代理（一次性）

```bash
# HTTP/HTTPS 代理
git config --global http.proxy http://127.0.0.1:7890
git config --global https.proxy http://127.0.0.1:7890

# 如果是 socks5 代理
git config --global http.proxy socks5://127.0.0.1:7890
git config --global https.proxy socks5://127.0.0.1:7890
```

> 端口号根据实际代理软件调整（常见：7890/1080/7897）。

---

## 2. 基础推送流程

```bash
# 首次推送（新仓库）
git push -u origin main

# 后续推送
git push

# 强制推送（覆盖远程，谨慎使用）
git push origin main --force

git tag v0.3.2-alpha && git push origin v0.3.2-alpha --force

git push origin main --force && git push origin v0.3.0-alpha --force
```

---

## 3. 推送 tag

```bash
# 推送单个 tag
git push origin v0.2.1

# 推送所有 tag
git push --tags

# 强制覆盖远程 tag
git push origin v0.2.1 --force
```

---

## 4. 推送前检查清单

```bash
# 1. 确认当前分支
git branch

# 2. 确认有要推送的 commit
git log --oneline -5

# 3. 确认 tag 已打（如有需要）
git tag -l

# 4. 编译检查（Go 项目）
go build ./...
```

---

## 5. 常见问题处理

### 5.1 推送被拒绝（non-fast-forward）

```
! [rejected]        main -> main (fetch first)
```

**原因：** 远程有本地没有的提交。

**解决方案：**

```bash
# 方案 A：拉取合并
git pull origin main --rebase
git push

# 方案 B：确认覆盖（本地是最新的情况下）
git push origin main --force
```

### 5.2 连接失败

```
fatal: unable to access '...': Failed to connect to github.com port 443
```

**原因：** 网络不通，需要代理。

**解决方案：**

```bash
# 检查代理是否设置
git config --global --get http.proxy

# 临时设置代理（仅当前命令生效）
http_proxy=http://127.0.0.1:7890 https_proxy=http://127.0.0.1:7890 git push

# 或者确认代理软件正在运行
```

### 5.3 代理设置后仍失败

```bash
# 检查代理是否正常工作
curl -x http://127.0.0.1:7890 https://github.com

# 尝试不同的代理端口
git config --global http.proxy http://127.0.0.1:7897
```

---

## 6. xtool 项目快速参考

```bash
# 进入项目目录
cd D:\Project\xtool

# 构建检查
go build ./...

# 运行测试
go test -v -count=1 -timeout 30s -run "^TestMemoryBackend" ./pool/

# 提交并打 tag
git add -A
git commit -m "描述改动内容"
git tag v0.2.1 -f

# 推送（需代理）
git push origin main --force
git push origin v0.2.1 --force
```

---

## 7. 代理环境变量方式（不修改 git 配置）

```bash
# 单条命令临时生效
$env:http_proxy="http://127.0.0.1:7890"; $env:https_proxy="http://127.0.0.1:7890"; git push

# 或 PowerShell 一行
$env:http_proxy="http://127.0.0.1:7890"; $env:https_proxy="http://127.0.0.1:7890"; git push origin main --force
```

---

*最后更新：2026-07-28*
